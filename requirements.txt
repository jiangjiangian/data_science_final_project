# Python deps for the canonical pipeline (`scripts/`). Pinned to ranges that
# scripts/run_all.py exercised in Colab. Install with:
#   pip install -r requirements.txt
#
# Numeric stack
numpy>=1.26
pandas>=2.1

# Scikit-learn family (tree ensembles + calibration + grid CV)
scikit-learn>=1.4
xgboost>=2.0
lightgbm>=4.1
shap>=0.44

# I/O + plotting + persistence
matplotlib>=3.8
seaborn>=0.13
joblib>=1.3
requests>=2.31  # used by step1b_fetch_weather.py (Open-Meteo)
