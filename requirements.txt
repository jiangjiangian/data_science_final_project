# Python deps for the canonical Colab notebooks
# (python/cpbl_pipeline.ipynb v1, python/cpbl_pipeline_v2.ipynb v2).
# Versions are the floors the notebook was last verified against.
# Install with:  pip install -r requirements.txt
#
# Numeric stack
numpy>=1.26
pandas>=2.1

# Scikit-learn family (tree ensembles + calibration + grid CV)
scikit-learn>=1.4
xgboost>=2.0
lightgbm>=4.1
shap>=0.44

# I/O + plotting + persistence
matplotlib>=3.8
seaborn>=0.13
joblib>=1.3
requests>=2.31   # Open-Meteo Archive (step1b weather fetch)
ipykernel>=6.29  # to run the notebooks locally with `jupyter` / `papermill`
